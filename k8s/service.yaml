apiVersion: v1
kind: Service
metadata:
  name: esp32-panel
  labels:
    app: esp32-panel
spec:
  type: ClusterIP
  selector:
    app: esp32-panel
  ports:
    - name: http
      port: 80
      targetPort: 80
